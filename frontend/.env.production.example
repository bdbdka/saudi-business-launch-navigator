# Browser-visible configuration only. This value is compiled into the frontend
# image and is not a secret. Replace the illustrative origin before deployment.
NEXT_PUBLIC_API_BASE_URL=https://api.navigator.example.invalid
NEXT_PUBLIC_CATALOG_MODE=PORTFOLIO_DEMO_CATALOG
